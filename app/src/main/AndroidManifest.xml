<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Своё уведомление при превышении лимита (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Запрос исключения из оптимизации батареи (агрессивные OEM-килдеры) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Синхронизация через сеть -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Голосовой ввод трат: запись с микрофона для распознавания речи -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Фоновая голосовая активация (wake-word): постоянный микрофон в foreground-сервисе -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Тактильный отклик на срабатывание слова-триггера -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 11+: без этого SpeechRecognizer не «видит» сервис распознавания на устройстве -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".SpendLimitApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.SpendLimit"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SpendLimit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Сервис перехвата уведомлений. BIND_NOTIFICATION_LISTENER_SERVICE
          выдаётся системой только после того, как пользователь вручную включит
          доступ в настройках (Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS).
        -->
        <service
            android:name=".service.notification.SpendNotificationListenerService"
            android:exported="false"
            android:label="@string/notif_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!--
          Фоновая голосовая активация (wake-word). type=microphone (требование Android 14).
          Стартовать только из видимой Activity — mic-FGS нельзя поднять из фона/по загрузке (API 31+).
        -->
        <service
            android:name=".ui.voice.wake.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>
</manifest>
