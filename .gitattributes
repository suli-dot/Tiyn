# Gradle wrapper: скрипт должен быть LF (иначе не запустится на Linux/macOS)
gradlew      text eol=lf
*.bat        text eol=crlf
*.jar        binary

# Прочие текстовые исходники — нормализуем перевод строк
*.kt         text
*.kts        text
*.xml        text
*.md         text
*.pro        text
*.toml       text
*.properties text
*.json       text
*.sql        text
